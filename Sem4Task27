// Задача 27: 
// Напишите программу, которая принимает на вход число и выдаёт сумму цифр в числе.


// Метод читает данные от пользователя
ReadDataStr  int(строка  msg)
{
    Консоль.Строка записи (msg);
    возвращает значение int .Синтаксический анализ(консоль.Строка чтения() ?? "0");
}

// Метод выводит результат пользователю
PrintData  void(строка  msg, int  res)
{
    Консоль.Строка записи (msg + res);
}


//Метод считает сумму цифр числа используя char

SummaNumDigChar  int (число чисел)
{
    0 = res int;
    // Конвертируем int число в строку
    num = строка str .toString();
    для    (int i=0; i  
    {   
        // Так как в char хрянятся символы в формате ASCII то мы отнимаем при каждой итерации 
        // значение ASCII - '0' = 48
        res = res + (str[i] - '0');
    }
    return res;
}

//Метод считает сумму цифр числа используя остаток от деления
Сумма  чисел int(int  num)
{
    0 = res int;
    в то время как (число > 0)
    {
        10 % num + res=res;
        10 / num =num;
    }
    возврат res ;
}


//Начало основного кода

//Вводим данные с консоли
ReadDataStr = number int("Введите положительное, целое число: ");

// Вычисляем сумму методом char
Дата -время= d1  Дата-время.Сейчас;
Сумма  чисел =  сумма чисел  int(число);


// Вычисляем сумму методом RemOfDig
Дата -время= d2  дата-время.Сейчас;
Сумма  чисел digremofdiv= Сумма чисел  int(число);


// Печатаем результат: 
PrintData("(Метод char) Сумма цифр числа равна: ", summaChar);
Консоль.Write("Время решения по методу Char равно: ");
Консоль.Строка записи(дата-время.Теперь  - d1);
Консоль.Строка записи("") ;
PrintData("(Метод RemOfDiv) Сумма цифр числа равна: ", summaChar);
Консоль.Write("Время решения по методу RemOfDig равно: ");
Консоль.Строка записи(дата-время.Теперь  - d2);
